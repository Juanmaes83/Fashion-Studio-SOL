async function request(fetchImpl,url,options){const response=await fetchImpl(url,options);const body=await response.json().catch(()=>({}));if(!response.ok)throw Object.assign(new Error(body.error?.message||`Provider request failed (${response.status})`),{code:'OPENAI_REQUEST_FAILED'});return body}
export async function analyzeWardrobe({fetchImpl=fetch,baseUrl,key,model,image}){const schema={type:'object',additionalProperties:false,properties:{items:{type:'array',maxItems:12,items:{type:'object',additionalProperties:false,properties:{name:{type:'string'},part:{type:'string',enum:['upperbody','wholebody_up','lowerbody','accessories_up','shoes']},color:{type:'string',pattern:'^#[0-9A-Fa-f]{6}$'},secondaryColor:{anyOf:[{type:'string',pattern:'^#[0-9A-Fa-f]{6}$'},{type:'null'}]},tags:{type:'array',items:{type:'string'},maxItems:8}},required:['name','part','color','secondaryColor','tags']}}},required:['items']};const body=await request(fetchImpl,`${baseUrl}/responses`,{method:'POST',headers:{authorization:`Bearer ${key}`,'content-type':'application/json'},body:JSON.stringify({model,input:[{role:'user',content:[{type:'input_text',text:'Identify every distinct wearable garment. Return concise catalog metadata only.'},{type:'input_image',image_url:`data:image/png;base64,${image.toString('base64')}`}]}],text:{format:{type:'json_schema',name:'wardrobe_items',strict:true,schema}}})});const text=body.output_text||body.output?.flatMap(x=>x.content||[]).find(x=>x.type==='output_text')?.text;if(!text)throw Object.assign(new Error('Provider returned no structured output'),{code:'OPENAI_EMPTY_RESULT'});return JSON.parse(text)}
export async function editImages({fetchImpl=fetch,baseUrl,key,model,quality='high',prompt,images,size='1024x1024'}){const form=new FormData();form.set('model',model);form.set('prompt',prompt);form.set('size',size);form.set('quality',quality);form.set('output_format','png');images.forEach((bytes,index)=>form.append('image[]',new Blob([bytes],{type:'image/png'}),`image-${index+1}.png`));const body=await request(fetchImpl,`${baseUrl}/images/edits`,{method:'POST',headers:{authorization:`Bearer ${key}`},body:form});const encoded=body.data?.[0]?.b64_json;if(!encoded)throw Object.assign(new Error('Provider returned no image'),{code:'OPENAI_EMPTY_IMAGE'});return Buffer.from(encoded,'base64')}
